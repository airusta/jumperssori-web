---
deployment:
  tasks:
    # Docroot real del subdominio staging (confirmado en cPanel, bajo public_html).
    - export DEPLOYPATH=/home2/jumperss/public_html/staging.jumperssori.com/
    - /bin/cp -R *.html "$DEPLOYPATH"
    - /bin/cp -R assets "$DEPLOYPATH" 2>/dev/null || true
